{% for vlan in vlans %}
vlan {{ vlan.id }}
 name {{ vlan.name }}
 mode fabricpath
{% endfor %}
{% set allowed_vlans = vlans|map(attribute='id')|list|join(',') %}
{% for t in trunks %}
interface {{ t }}
 switchport trunk allowed vlan add {{ allowed_vlans }}
{% endfor %}